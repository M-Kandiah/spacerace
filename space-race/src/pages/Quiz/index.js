import React from 'react'

const Quiz = () => {
    let amt;
    let category;
    let difficulty;
    // categories are ids going from 9 to 32
    const url = `https://opentdb.com/api.php?amount=${amt}&category=${category}&difficulty=${difficulty}&type=multiple`;
    return (
        <div>
            
        </div>
    )
}

export default Quiz;
